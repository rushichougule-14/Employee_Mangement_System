class User
{
    constructor(userId,fullName,email,password)
    {
        this.userId = userId;
        this.fullName = fullName;
        this.email = email;
        this.password = password;
    }
}

module.exports=User;